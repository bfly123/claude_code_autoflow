# 贡献指南

感谢您考虑为 Claude Code 渐进式任务管理系统做出贡献！

## 如何贡献

### 报告问题

如果您发现 bug 或有功能建议：

1. 检查 [Issues](https://github.com/yourusername/claude-progressive-task-system/issues) 确认问题未被报告
2. 创建新 Issue，包含：
   - 清晰的标题和描述
   - 重现步骤（如果是 bug）
   - 期望行为 vs 实际行为
   - Claude Code 版本信息
   - 相关日志或截图

### 提交代码

1. **Fork 仓库**
   ```bash
   git clone https://github.com/yourusername/claude-progressive-task-system.git
   cd claude-progressive-task-system
   ```

2. **创建分支**
   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/your-bug-fix
   ```

3. **编写代码**
   - 遵循现有代码风格
   - 保持代码简洁（参考 CLAUDE.md）
   - 添加必要的注释
   - 确保向后兼容

4. **测试更改**
   - 在 Claude Code 中测试命令
   - 确保不影响现有功能
   - 验证 todo.md 格式正确

5. **提交更改**
   ```bash
   git add .
   git commit -m "feat: add amazing feature"
   # 或
   git commit -m "fix: resolve issue with step transition"
   ```

6. **推送并创建 PR**
   ```bash
   git push origin feature/your-feature-name
   ```
   然后在 GitHub 上创建 Pull Request

## 代码规范

### 命令文件格式

所有命令文件（`commands/*.md`）应遵循以下结构：

```markdown
---
description: 命令描述
argument-hint: [参数提示]
---

命令的详细说明

## Phase 1: 第一步
...

## Phase 2: 第二步
...
```

### 命名约定

- 命令文件：小写，连字符分隔（如 `plan.md`）
- 变量：描述性命名（如 `current_step`, `substep_count`）
- 步骤标记：
  - `[▶️]` - 当前步骤
  - `[x]` - 已完成
  - `[ ]` - 待完成

### 注释规范

- 保持注释简洁
- 解释"为什么"，而非"是什么"
- 在复杂逻辑处添加注释
- 避免过度注释

## 提交信息格式

使用语义化提交信息：

```
<type>: <subject>

<body>

<footer>
```

**Type**:
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 格式调整（不影响代码）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具相关

**示例**:
```
feat: add auto step transition in /run command

- Automatically move to next step after completion
- Support both Simple and Complex steps
- Update todo.md format accordingly

Closes #123
```

## 测试指南

在提交前，请测试：

1. **基本工作流**
   ```bash
   /plan Test task with 3 steps
   /run
   /clear
   /expand  # 如果是 Complex
   /run
   /progress
   ```

2. **边界情况**
   - Simple 步骤是否正确跳过展开
   - Complex 步骤完成后是否自动切换
   - 审查分数 <28 时的处理
   - todo.md 格式是否正确

3. **兼容性**
   - 与现有项目兼容
   - 跨会话恢复

## 文档更新

如果您的更改影响用户体验，请更新：

- `README.md` - 使用说明
- `PLAN.md` - 设计文档
- 相关命令的 `.md` 文件

## 版本发布

项目使用语义化版本：`MAJOR.MINOR.PATCH`

- **MAJOR**: 不兼容的 API 更改
- **MINOR**: 向后兼容的功能添加
- **PATCH**: 向后兼容的 bug 修复

## 行为准则

### 我们的承诺

为了营造开放和友好的环境，我们承诺：

- 使用友好和包容的语言
- 尊重不同的观点和经验
- 优雅地接受建设性批评
- 关注对社区最有利的事情
- 对其他社区成员表示同理心

### 不可接受的行为

- 使用性化的语言或图像
- 挑衅、侮辱或贬损的评论
- 公开或私下的骚扰
- 未经许可发布他人的私人信息
- 其他不道德或不专业的行为

## 获取帮助

如有疑问：

- 📋 查看 [Issues](https://github.com/yourusername/claude-progressive-task-system/issues)
- 💬 参与 [Discussions](https://github.com/yourusername/claude-progressive-task-system/discussions)
- 📧 联系维护者

---

再次感谢您的贡献！🎉
