---
description: "[DEPRECATED] Auto-expansion now handled by /run"
---

**⚠️ DEPRECATED in v3.0**

This command is no longer needed. `/run` automatically expands Complex steps.

**Migration**: Use `/run` instead - it auto-expands when needed.

---

For reference, expansion logic is now in `/run` Phase 2.
